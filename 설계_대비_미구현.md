# 결대로 — 설계에는 있으나 아직 없는 것

작성 2026-09-11 · 기준 커밋 `fix: 숙소별 동선 미반영 버그 수정 및 안정성·보안 점검 반영`

발표자료(`결대로_발표_최종`)·`CLAUDE.md`·`결대로_개발기획서_v2`에 적혀 있으나 **현재 코드에 없는 것**을 모았다.
각 항목은 "왜 설계에 있었나 → 지금은 어떻게 돌아가나 → 넣는다면 무엇이 좋아지나" 순서로 적었다.

> 발표자료는 2026-09-11자로 현행화해 이 항목들이 **구현된 것처럼 보이지 않게** 고쳐 두었다.
> 이 문서는 "다음에 만들 것" 목록이지, 발표에서 감춰야 할 목록이 아니다.

---

## A. 지금 바로 값이 나오는 것

### A-1. 입력만 받고 쓰이지 않는 설정 — 예산 · 인원

S3b(세부 조건)에서 **하루 교통 예산**과 **동행 인원**을 입력받지만, 두 값은 어떤 알고리즘·API에도 들어가지 않는다.

```bash
# 알고리즘·API 어디에서도 참조하지 않음 (결과 없음)
grep -rn "budget\|people" src/algorithms/ src/api/
```

`TripSettings`에 담겨 저장까지 되지만 **결과에 아무 영향이 없다.** 사용자는 조정해도 일정이 그대로라 혼란스럽다.

**선택지**
1. 실제로 반영 — 예산은 `getTransportMode()`에 합류(예산이 낮으면 대중교통 고정), 인원은 식당 추천 필터나 예상 비용 표시에 사용
2. 또는 화면에서 제거 — 안 쓰는 입력을 지우는 것도 정직한 개선

지금 상태(받되 안 씀)가 가장 나쁘다. **둘 중 하나는 해야 한다.**

---

### A-2. 죽은 라우트 — `SavedDetail`

`RootStackParamList`에 선언돼 있으나 화면 파일도, `App.tsx` 등록도 없다.

```ts
// src/types/index.ts:70
SavedDetail: { scheduleId: string };   // ← 대응 화면 없음
```

`CLAUDE.md`의 화면표에는 **S6 `SavedDetailScreen` — 저장된 일정 상세 보기 + 영업중 링크**로 적혀 있다. 실제로는 저장 목록에서 카드를 누르면 곧바로 `Timeline`으로 가고, 영업시간은 별도 `BusinessHoursScreen`이 담당하게 바뀌었다.

**할 일**: 타입에서 `SavedDetail` 제거 (설계 잔재). 지금 구조가 더 낫다 — 화면을 새로 만들 이유는 없다.

---

### A-3. 여행 기간 표기 함수가 세 곳에 흩어져 있음

`TimelineScreen`이 일수를 박수로 잘못 읽어 "2박 3일"로 표시하던 버그를 2026-09-11에 고쳤다(`TROUBLESHOOTING.md` 참고). 그런데 **같은 계산식이 여전히 세 화면에 복사돼 있다.**

| 파일 | 코드 |
|---|---|
| `TimelineScreen.tsx` | `` `${d - 1}박 ${d}일` `` |
| `SavedListScreen.tsx` | `` `${days - 1}박${days}일` `` (공백 없음) |
| `WeatherScreen.tsx` | `` `${days - 1}박 ${days}일` `` (두 군데) |

**할 일**: `src/utils/formatTripDays.ts`로 추출해 한 곳만 고치면 되게 만든다. 표기 공백도 통일된다.

---

### A-4. 선택한 명소가 조용히 누락되는 문제

`calcTripDays()`는 전체를 **총합**으로 계산하는데 `generateTimeline()`은 하루씩 **그리디로 채우다 `break`** 한다. 마지막 날에서 끊기면 남은 명소는 안내 없이 사라진다.

재현: 명소 4곳(협재·섭지코지·산굼부리·천제연폭포) 선택 → 헤더는 "명소 4곳", 타임라인에는 3곳만.

**할 일**: 배치 후 남은 명소가 있으면 일수를 늘려 재배치하거나, 못 담은 명소를 화면에 알린다. 상세는 `TROUBLESHOOTING.md` 참고.

---

## B. 설계에 있던 구조 — 지금은 없지만 언젠가 필요한 것

### B-1. 백엔드 서버 (Node.js + Express)

**설계**: 4계층 아키텍처의 한 층. 알고리즘을 서버에 두고 앱은 호출만.
**현재**: 없음. 알고리즘 5종이 전부 기기 안에서 돈다. 앱이 외부 API 3종을 직접 호출한다.

지금 구조가 **더 낫다** — 서버 비용·배포·장애 지점이 없고, 오프라인에서도 계산이 된다. 계산량도 명소 수십 개 수준이라 기기에서 충분하다.

**다만 딱 하나, 서버가 반드시 필요해지는 시점이 있다**: Claude API 연동(B-2). API 키를 클라이언트에 두면 그대로 유출되므로, 키를 환경변수로 들고 있는 최소 엔드포인트가 필요하다. 이미 `TODO.md`에 적혀 있다.

> 발표에서 "백엔드가 없다"는 약점이 아니라 **선택**으로 설명하는 편이 정확하다.
> 다만 이 프로젝트는 이미 공개 저장소에 API 키를 유출한 적이 있다(`TROUBLESHOOTING.md`).
> **지금도 `apiKeys.ts`의 키 3종은 클라이언트 번들에 들어간다** — 배포 시에는 이 부분을 짚어야 한다.

---

### B-2. Claude API — 자유 텍스트 → 조건 구조화

**설계·TODO**: "자동 설정" 플로우에서 "3박 4일로 애들이랑 조용한 데 위주로" 같은 문장을 입력받아 `TripSettings`로 변환.
**현재**: "자동 설정"과 "직접 설정"이 사실상 같은 화면(S3a→S3b)으로 간다. 두 트랙의 차이가 거의 없다.

**넣으면**: 앱 이름이 내건 "AI 동선 플래너"의 AI가 실제로 드러난다. 지금 AI에 해당하는 건 알고리즘 5종이고, 이건 **자동화**지 언어 이해가 아니다. 심사에서 "AI가 어디에 쓰였나"를 물으면 가장 아픈 지점.
**선행 조건**: B-1의 최소 백엔드.

---

### B-3. SQLite 명소 캐시 (`expo-sqlite`)

**설계**: `categories` · `spots` · `spot_themes` 3개 테이블, TourAPI에서 시딩 후 7일 주기 갱신.
**현재**: 없음. 앱을 켤 때마다 TourAPI·카카오를 호출하고, 실패하면 번들에 내장된 `jejuSpots.ts` 34개로 폴백한다.

**넣으면**
- 재실행 시 로딩이 빨라진다 (지금은 S4 진입 때마다 수 초 대기)
- API 호출량이 줄어 **일일 쿼터 초과 위험이 낮아진다** (data.go.kr은 쿼터가 있다)
- 비행기 모드에서도 최신 명소를 볼 수 있다

**비용**: 의존성 1개 + 마이그레이션 관리. 지금 규모(명소 100개 미만)에서는 AsyncStorage에 JSON 배열로 캐싱하는 것만으로도 대부분의 이득을 얻는다. **먼저 그걸 해보고 부족하면 SQLite로 가는 순서**를 권한다.

---

### B-4. 지도 화면 (카카오맵 WebView)

**설계**: `react-native-webview`로 카카오맵 표시.
**현재**: 없음. 동선은 타임라인 텍스트로만 보여주고, 위치 확인은 카카오맵 링크로 외부 앱에 넘긴다.

**넣으면**: "최단 동선을 계산한다"는 핵심 가치가 **눈에 보인다.** 지금은 73분·97분 같은 숫자로만 전달되는데, 지도 위 경로 선 하나가 훨씬 설득력이 있다. 발표 데모 효과도 가장 크다.

**주의**: Expo Go에서 WebView는 동작하지만 카카오맵 JS SDK는 도메인 등록이 필요하다. 대안으로 정적 이미지 지도(카카오 Static Map API)를 쓰면 훨씬 간단하다.

---

## C. 개발·배포 환경

### C-1. EAS Build — 독립 실행 앱

**설계**: 발표자료 3쪽 김태겸 역할에 "EAS Build · 실기기 테스트", 22쪽 로드맵에도 포함.
**현재**: Expo Go로만 구동. `.apk`/`.ipa` 빌드는 없다.

**넣으면**: 심사위원이 **Expo Go 설치 없이** QR·링크로 바로 실행해볼 수 있다. 1차 심사 제출물로 앱을 낸다면 사실상 필수.
**비용**: EAS 계정 + 빌드 대기(무료 티어는 큐가 길다). **마감 전에 미리 한 번 돌려봐야 한다** — 처음 빌드는 설정 오류로 실패하기 쉽다.

---

### C-2. 상태 관리 · 데이터 페칭 라이브러리

**설계**: Zustand(전역 상태), Axios + React Query(API 캐싱).
**현재**: 둘 다 없음. 상태는 화면 간 `navigation.navigate(..., params)`로 전달하고, API는 `axios` 직접 호출 + `useState`.

지금 규모에선 **문제없다.** 화면 10개, 흐름이 선형이라 전역 상태가 필요할 만큼 복잡하지 않다.

다만 **React Query는 값이 있다** — B-3(캐싱)과 목적이 겹치고, 자동 재시도·로딩 상태 관리가 따라온다. 카카오/기상청 호출이 실패했을 때 지금은 조용히 폴백하거나 빈 화면이 되는데, 재시도와 에러 표시가 붙는다.

---

### C-3. 푸시 알림

`TODO.md`의 "추후 고려" 항목 — 여행 전날 일정 알림. 저장된 일정에 `startDate`가 이미 있으므로 데이터는 준비돼 있다. `expo-notifications` 추가로 구현 가능.

우선순위는 낮다. **여행 계획을 세우는 것**이 이 앱의 핵심이고, 알림은 그 다음 문제다.

---

## D. 문서 정합성

### D-1. `CLAUDE.md`가 설계 시점에 멈춰 있음

Claude Code가 매 세션 읽는 프로젝트 가이드인데 **현재 코드와 어긋난 내용이 많다.**

| `CLAUDE.md` 기재 | 실제 |
|---|---|
| BackEnd: Node.js + Express | 없음 (서버리스) |
| DB: SQLite 명소 캐시 | 없음 |
| 화면 8개, S6 = `SavedDetailScreen` | 화면 10개, `SavedDetailScreen` 없음 |
| `calcTripDays`가 이동시간 `(n-1) × 20분` | `estimateTravelMinutes()` 거리 기반 |
| `Spot.category` 5종 | 6종 (`activity` 추가) |
| `accommodation` 6종 (`airport`·`east`·`west`) | 7종 (`aewol`·`hallim`·`jungmun`·`seongsan`) |

**이대로 두면 AI 페어 코딩이 없는 구조를 있다고 가정한다.** 실제로 이 문서 때문에 잘못된 코드가 제안될 수 있다.

**할 일**: `CLAUDE.md`를 현재 구조로 갱신. 우선순위 높음 — 비용은 낮고 영향은 크다.

### D-2. `README.md` 수치가 낡음

- "Expo SDK 54" → 실제 **57** (2026-09-09 업그레이드)
- "화면 컴포넌트 (8개)" → 실제 **10개**
- "제주 명소 시드 데이터 (28개)" → 실제 **34개**
- 팀원 역할 표가 비어 있음 (`-`)

### D-3. `TODO.md` 완료 항목 정리

"진행 예정 / 카카오 Local API 연동 (나중에)" 섹션이 **전부 `[x]` 완료 상태**인데 여전히 "진행 예정" 아래에 있다. 완료 섹션으로 옮기면 남은 일이 한눈에 보인다.

---

## 권장 순서

마감(2026-09-21)을 감안한 현실적 우선순위.

| 순위 | 항목 | 이유 | 드는 시간 |
|---|---|---|---|
| 1 | **D-1 `CLAUDE.md` 갱신** | 앞으로의 모든 작업 품질에 영향 | 30분 |
| 2 | **A-4 명소 누락** | 데모 중 드러나면 치명적 | 1~2시간 |
| 3 | **A-1 예산·인원** | 조작해도 반응 없는 입력은 심사에서 질문 대상 | 1시간 (제거면 20분) |
| 4 | **C-1 EAS Build** | 제출 형태를 좌우, 처음이라 실패 여지 큼 → 일찍 | 반나절 |
| 5 | **B-4 지도 화면** | 데모 설득력 최대 | 1일 (정적 지도면 반나절) |
| 6 | A-2 · A-3 · D-2 · D-3 | 정리 작업, 짬날 때 | 각 30분 |
| — | B-1 · B-2 · B-3 · C-2 · C-3 | 마감 이후 | — |

---

## 참고

- 구현 중 마주친 문제와 해결: `TROUBLESHOOTING.md` (14건)
- 남은 작업 체크리스트: `TODO.md`
- 발표자료 현행화 내역: `결대로_발표_구현반영_20260911.pptx` (`~/Downloads`)

# 결대로 (Gyeoldaero) 프로젝트 정리

## 1. 프로젝트 한눈에 보기

`결대로`는 사용자의 여행 취향과 조건을 바탕으로 제주도 여행 동선을 구성해주는 모바일 여행 플래너 앱입니다.

이 프로젝트의 핵심 목표는 다음과 같습니다.

- 사용자가 선호하는 여행 스타일을 선택한다.
- 일정 시간, 인원, 예산, 짐 무게 같은 조건을 입력한다.
- 제주 명소를 직접 고르거나 추천 기반으로 고른다.
- 선택된 명소를 바탕으로 여행 일수와 동선을 계산한다.
- 하루 단위 타임라인 형태로 여행 일정을 보여준다.
- 생성된 일정을 앱 내부에 저장하고 다시 불러온다.

현재 저장소의 구현 형태는 `React Native + Expo` 기반의 모바일 앱이며, iOS와 Android에서 동일한 흐름으로 사용할 수 있도록 구성되어 있습니다.

---

## 2. 이 프로그램이 해결하려는 문제

제주 여행을 준비할 때 보통 다음과 같은 불편이 생깁니다.

- 어디를 갈지 정하는 데 시간이 오래 걸린다.
- 선택한 장소들이 실제로 이동 가능한 순서인지 판단하기 어렵다.
- 일정 시간 안에 모두 소화 가능한지 계산하기 어렵다.
- 식사, 숙소 복귀, 첫날 도착 시간, 마지막 날 출발 시간 같은 현실적인 조건이 빠지기 쉽다.

`결대로`는 이 문제를 줄이기 위해, 사용자의 취향과 제약 조건을 입력받아 여행 흐름을 자동으로 정리하는 것을 목표로 합니다.

---

## 3. 현재 프로젝트 상태

현재 `main` 브랜치 기준으로 다음이 구현되어 있습니다.

- 앱 기본 화면 흐름 구현
- 여행 스타일 및 세부 조건 입력 UI 구현
- 제주 명소 선택 화면 구현
- 일정 자동 생성 알고리즘 구현
- 타임라인 결과 화면 구현
- 로컬 저장소 기반 일정 저장/불러오기 구현
- 한국관광공사 TourAPI 연동 코드 구현

현재 계획만 있고 아직 미완성인 항목도 있습니다.

- 날씨 전용 화면
- 기상청 API 연동
- 카카오 Local API 연동
- 실제 맛집 추천 고도화
- 실제 이동 시간 정밀화

---

## 4. 타깃 사용자

이 앱은 아래와 같은 사용자에게 적합합니다.

- 제주 여행 초안을 빠르게 만들고 싶은 사용자
- 이동 동선이 꼬이지 않는 일정이 필요한 사용자
- 취향 기반으로 여행 코스를 구성하고 싶은 사용자
- 저장된 일정을 다시 열어보고 싶은 사용자

---

## 5. 기술 스택

현재 코드 기준 핵심 기술 스택은 다음과 같습니다.

- 프레임워크: `React Native`
- 앱 실행 환경: `Expo SDK 54`
- 언어: `TypeScript`
- 네비게이션: `@react-navigation/native`, `@react-navigation/stack`
- 로컬 저장소: `@react-native-async-storage/async-storage`
- HTTP 통신: `axios`
- SVG 렌더링: `react-native-svg`
- 런타임: `React 19.1.0`

`package.json` 기준 주요 의존성은 다음과 같습니다.

- `expo`
- `react-native`
- `react`
- `axios`
- `@react-navigation/native`
- `@react-navigation/stack`
- `@react-native-async-storage/async-storage`
- `react-native-svg`

---

## 6. 실행 형태와 개발 방식

이 프로젝트는 현재 전형적인 네이티브 Xcode 프로젝트를 직접 관리하는 구조가 아니라, `Expo managed workflow` 중심으로 사용됩니다.

즉, 개발자는 보통 아래 방식으로 확인합니다.

- `npm install`
- `npx expo start`
- `Expo Go` 앱 또는 iOS 시뮬레이터로 실행

현재 저장소 루트에는 `ios/` 또는 `android/` 네이티브 폴더가 없기 때문에, 처음부터 Xcode 프로젝트를 직접 열어 수정하는 방식보다는 Expo 개발 서버를 통해 확인하는 흐름이 기본입니다.

---

## 7. 주요 화면 구성

앱 진입점은 `App.tsx`이며, 스택 네비게이션 기반으로 화면이 연결되어 있습니다.

현재 코드에 등록된 주요 화면은 다음과 같습니다.

### 7.1 SplashScreen

- 앱 시작 시 보여주는 스플래시 화면
- 서비스의 첫 인상을 담당

### 7.2 SavedListScreen

- 저장된 일정 목록을 보여주는 화면
- 사용자가 이전에 저장한 여행 계획을 다시 확인할 수 있음

### 7.3 HomeScreen

- 여행을 어떤 방식으로 시작할지 선택하는 화면
- 직접 설정과 자동 설정 두 가지 진입점을 제공
- 저장된 일정 보기로도 이동 가능

### 7.4 TravelStyleScreen

- 사용자의 여행 취향을 선택하는 화면
- 예: 힐링, 액티비티, 음식, 문화, 사진, 야간 감성 같은 성향

### 7.5 DetailConditionScreen

- 세부 여행 조건을 받는 화면
- 시작 시간, 종료 시간, 예산, 인원, 짐 무게 등 조건 입력

### 7.6 SpotSelectScreen

- 제주 명소를 선택하는 화면
- 직접 고르거나 API 기반 데이터를 활용하는 중심 화면

### 7.7 TimelineScreen

- 생성된 일정을 타임라인으로 보여주는 결과 화면
- 하루 단위로 여행 흐름을 확인할 수 있음

### 7.8 BusinessHoursScreen

- 명소 관련 외부 링크나 영업시간 확인 흐름을 담당하는 화면

---

## 8. 사용자 흐름

현재 코드와 문서를 바탕으로 한 기본 사용자 흐름은 아래와 같습니다.

1. 앱 실행
2. 스플래시 진입
3. 저장 목록 또는 새 여행 시작 선택
4. 홈 화면에서 직접 설정 또는 자동 설정 선택
5. 여행 스타일 선택
6. 세부 조건 입력
7. 명소 선택
8. 일정 자동 생성
9. 타임라인 결과 확인
10. 일정 저장 또는 외부 링크 확인

---

## 9. 핵심 기능 설명

### 9.1 취향 기반 일정 설계

사용자가 여행 테마를 선택하면, 이후 일정 구성의 기준 데이터로 활용됩니다.

현재 타입 정의상 사용되는 주요 테마는 다음과 같습니다.

- `healing`
- `activity`
- `food`
- `culture`
- `photo`
- `night`

### 9.2 세부 조건 기반 일정 현실화

단순히 명소만 나열하는 것이 아니라, 다음과 같은 현실적인 조건을 반영하도록 설계되어 있습니다.

- 하루 시작 시간
- 하루 종료 시간
- 첫날 도착 시간
- 마지막 날 출발 시간
- 인원 수
- 예산
- 짐 무게

### 9.3 명소 데이터 활용

명소는 두 가지 축으로 다뤄집니다.

- 프로젝트 내부 시드 데이터
- 외부 관광 API 데이터

프로젝트에는 `src/data/jejuSpots.ts`가 있어 기본 제주 명소 데이터를 포함하고 있으며, TourAPI 연동 실패 시 fallback 데이터로 활용될 수 있게 설계되어 있습니다.

### 9.4 일정 자동 생성

사용자가 선택한 명소 목록과 숙소 기준 위치를 토대로, 방문 순서를 정렬하고 하루 단위 일정으로 분배합니다.

자동 생성 시 반영되는 요소는 다음과 같습니다.

- 숙소 또는 출발 기준 좌표
- 명소 간 거리
- 각 명소 예상 체류 시간
- 식사 시간 삽입
- 하루 가용 시간
- 첫날과 마지막 날의 부분 일정

### 9.5 일정 저장

생성된 일정은 로컬 저장소에 저장됩니다.

- 저장 방식: `AsyncStorage`
- 저장 키: `gyeoldaero_schedules`

이 구조 덕분에 앱을 다시 열어도 저장된 일정 목록을 다시 볼 수 있습니다.

---

## 10. 핵심 알고리즘 구조

`src/algorithms` 폴더에는 일정 계산과 관련된 핵심 로직이 들어 있습니다.

### 10.1 haversine.ts

- 두 좌표 간의 직선 거리를 계산하는 유틸리티
- 지구 곡률을 반영한 Haversine 공식을 사용

### 10.2 nearestNeighbor.ts

- 가장 가까운 다음 명소를 순차적으로 선택하는 방식
- 간단한 최근접 이웃 탐욕 알고리즘
- 완전한 TSP 최적해는 아니지만, 빠르고 직관적인 동선 생성에 적합

### 10.3 timeBudget.ts

- 전체 명소와 시간 조건을 고려해 필요한 여행 일수를 계산

### 10.4 generateTimeline.ts

- 이 프로젝트의 일정 생성 핵심 로직
- 명소 순서를 받아 하루 단위 일정으로 분해
- 점심/저녁 식사를 자동으로 끼워 넣음
- 숙소 출발, 이동, 명소 방문, 숙소 복귀 또는 공항 출발 흐름을 생성

현재 로직상 타임라인은 다음과 같은 아이템 타입을 사용합니다.

- `spot`
- `meal`
- `accommodation`
- `move`

---

## 11. 데이터 모델

프로젝트의 주요 타입은 `src/types/index.ts`에 정의되어 있습니다.

### 11.1 Spot

명소 단위 데이터입니다.

포함 정보 예시:

- 고유 ID
- 명소 이름
- 카테고리
- 위도/경도
- 예상 체류 시간
- 이미지 URL
- 이모지
- 태그
- 영업시간 링크

### 11.2 TripSettings

사용자의 여행 설정 정보입니다.

포함 정보 예시:

- 테마 배열
- 날씨
- 계절
- 하루 시작/종료 시간
- 첫날 도착 시각
- 마지막 날 출발 시각
- 인원 수
- 예산
- 짐 무게

### 11.3 TripSchedule

한 번 생성된 전체 일정 단위입니다.

포함 정보 예시:

- 일정 ID
- 일정 이름
- 생성 시각
- 여행 일수
- 선택 명소 목록
- 숙소 지역
- 태그
- 사용자 설정
- 일자별 계획

### 11.4 DayPlan / TimelineItem

타임라인 화면 렌더링에 직접 쓰이는 구조입니다.

- `DayPlan`: 하루치 일정 묶음
- `TimelineItem`: 특정 시간대의 한 활동

---

## 12. 외부 API 구조

현재 코드에서 실제 사용이 확인되는 외부 API는 한국관광공사 TourAPI입니다.

### 12.1 한국관광공사 TourAPI

관련 파일:

- `src/api/tourApi.ts`

용도:

- 제주 지역 기반 명소 조회
- 카테고리별 명소 조회
- GPS 기반 주변 명소 조회
- 명소 상세 링크 조회

사용 엔드포인트 예시:

- `areaBasedList2`
- `locationBasedList2`
- `detailCommon2`

현재 코드가 필요로 하는 키:

- `TOUR_API_KEY`

중요한 점:

- 이 키는 저장소에 포함되어 있지 않음
- `src/constants/apiKeys.ts` 파일을 로컬에서 별도 생성해야 함
- `.gitignore`에 해당 파일이 제외되어 있어 Git에 올라가지 않도록 처리되어 있음

### 12.2 향후 예정 API

`TODO.md` 기준 예정된 API는 다음과 같습니다.

- 기상청 단기예보 API
- 카카오 Local API

예상 활용 목적:

- 날씨 기반 여행일 선택
- 주변 맛집 추천
- 실제 이동 시간 보정

---

## 13. 저장 구조

로컬 저장 관련 코드는 `src/storage/scheduleStorage.ts`에 있습니다.

제공 기능:

- 일정 저장
- 전체 일정 불러오기
- 일정 삭제
- ID 기반 단일 일정 불러오기

현재 서버 DB 없이 앱 내부 로컬 스토리지만 사용하므로, 개인 기기에서 빠르게 테스트하기 쉽다는 장점이 있습니다.

---

## 14. 디자인 시스템

프로젝트는 `src/constants/theme.ts`를 통해 공통 디자인 값을 관리합니다.

문서와 코드 기준 주요 디자인 방향은 다음과 같습니다.

- 차분한 제주 자연색 기반 팔레트
- 카드형 레이아웃
- 모바일 중심 터치 인터페이스
- 일정/동선 정보가 한눈에 보이는 구조

README에 명시된 대표 색상 예시는 다음과 같습니다.

- `primary`: 주요 버튼/헤더
- `background`: 앱 전체 배경
- `surface`: 카드 배경
- `text`: 본문 텍스트
- `textMuted`: 보조 텍스트
- `warning`: 식사 타임라인 포인트
- `teal`: 숙소/이동 관련 포인트

---

## 15. 프로젝트 폴더 구조

```text
gyeoldaero-main/
├── App.tsx
├── app.json
├── index.ts
├── README.md
├── TODO.md
├── PROJECT_OVERVIEW.md
├── src/
│   ├── algorithms/
│   ├── api/
│   ├── components/
│   ├── constants/
│   ├── data/
│   ├── screens/
│   ├── storage/
│   └── types/
└── assets/
```

각 폴더 역할은 다음과 같습니다.

- `algorithms`: 동선 계산, 일정 분배 로직
- `api`: 외부 API 연동 코드
- `components`: 재사용 UI 컴포넌트
- `constants`: 디자인 토큰과 상수
- `data`: 시드 데이터
- `screens`: 화면 단위 컴포넌트
- `storage`: 로컬 저장 처리
- `types`: 공통 타입 정의

---

## 16. 개발 환경 세팅 방법

처음 실행 기준 권장 절차는 다음과 같습니다.

### 16.1 필수 준비물

- Node.js 18 이상
- npm
- Xcode 또는 iOS 시뮬레이터 사용 환경
- 또는 실제 iPhone + Expo Go

### 16.2 설치 및 실행

```bash
cd /Users/mac_k/캡스톤/gyeoldaero-main
npm install
npx expo start
```

### 16.3 iOS에서 확인하는 방법

- 실제 기기: `Expo Go` 앱으로 QR 스캔
- 시뮬레이터: Expo 실행 후 터미널에서 `i` 입력

---

## 17. 현재 확인된 주의사항

문서를 작성하는 시점에서 코드 기준으로 확인되는 주의사항은 다음과 같습니다.

### 17.1 API 키 파일 없음

- `src/constants/apiKeys.ts`가 저장소에 없음
- 따라서 TourAPI 기능은 바로 동작하지 않을 수 있음

### 17.2 일부 기능은 TODO 상태

- 날씨 화면과 기상청 API는 아직 미구현
- 카카오 Local API도 아직 연동 전

### 17.3 라우트 정의와 실제 등록 화면 차이 가능성

타입에는 `SavedDetail` 같은 항목이 있으나, 현재 `App.tsx`에 실제 스크린 등록 여부를 계속 맞춰봐야 합니다.

즉, 문서와 코드, 타입 정의를 앞으로 한 번 더 정리하는 작업이 필요할 수 있습니다.

---

## 18. 이 프로젝트의 강점

- 여행 취향과 현실 조건을 함께 고려한다.
- 단순 리스트가 아니라 시간 흐름 중심 일정 결과를 제공한다.
- 로컬 저장 기능이 있어 프로토타입 테스트가 쉽다.
- 제주 특화 여행 플래너라는 주제가 분명하다.
- 외부 관광 데이터 연동 확장성이 이미 열려 있다.

---

## 19. 다음 작업 추천

프로젝트를 더 완성도 있게 만들기 위해 추천하는 다음 단계는 아래와 같습니다.

1. `TOUR_API_KEY` 연결
2. `SpotSelectScreen`의 실제 API 데이터 동작 확인
3. 날씨 전용 화면 `WeatherScreen` 추가
4. 기상청 API 연동
5. 카카오 Local API로 맛집/이동시간 고도화
6. 타입 정의와 실제 네비게이션 구조 동기화
7. README와 본 문서를 기준으로 팀 공통 온보딩 문서 체계화

---

## 20. 한 줄 요약

`결대로`는 사용자의 취향과 조건에 맞춰 제주 여행 동선을 자동 설계하고, 결과를 타임라인으로 보여주며, 로컬에 저장할 수 있는 Expo 기반 모바일 여행 플래너 앱입니다.

# 결대로 (Gyeoldaero) — 프로젝트 진행 기록

> **마지막 업데이트**: 2026-07-08 (3차)
> **슬로건**: 당신의 결을 따라, 여행의 결을 설계합니다
> **한 줄 소개**: 여행 취향과 제약(짐·날씨·시간)을 입력하면 AI가 최적 동선과 적정 기간을 자동 설계하는 **제주 여행 AI 동선 플래너**

---

## 1. 전체 완성도 요약

| 영역 | 진척 | 비고 |
|---|---|---|
| 화면·플로우 구현 | ~95% | 11개 화면 전부 구현·연결, 타임라인 편집까지 |
| 핵심 알고리즘 | ~95% | 4종 + 테마 추천 점수 + 유닛테스트 + 버그수정 |
| 외부 API 연동 | ~90% | 5개 API 연동 · **날씨/지오코딩/지도/사진 실기기 검증됨** |
| 데이터/저장 | ~95% | 시드 61 + TourAPI 병합, 이미지 캐시, env 키관리 |
| 완성도(로딩·에러·빈상태) | ~85% | 공통 StateView, 키보드 회피 |
| **실기기 QA** | **~60%** | 핵심(지도·사진·날씨·담기) 확인 ✅ · 잔여: 예보저장·공유·편집 |
| 테스트 커버리지 | ~55% | **60 tests / 11 suites** (알고리즘·유틸·스토리지·테마·병합·캐시·계절) |
| 프로덕션 패키징 | ~70% | 아이콘·스플래시·**eas.json·번들ID·빌드가이드 완료** · 빌드 실행만 남음(Expo 로그인 필요) |

- **"만들어졌다" 기준: ~90%** / **"실제 동작 확정" 기준: ~80%**

---

## 2. 기술 스택

- **프론트**: React Native + Expo SDK 54, TypeScript, React Navigation v7(Stack)
- **저장**: AsyncStorage (일정 + 명소 사진 캐시)
- **지도**: react-native-webview + 네이버 지도 JS Dynamic Map (도로경로·핀 겹침 해소)
- **외부 API**: 한국관광공사 TourAPI, 기상청 단기·중기예보, 네이버 지도(지오코딩·길찾기·경로), AI 플래너(자체 서버)
- **테스트**: Jest 29 + ts-jest
- **브랜딩**: 물결 로고 앱 아이콘·스플래시 (app.json 적용 완료)

---

## 3. 화면 구성 (11개, 전부 등록·연결됨)

| ID | 화면 | 상태 | 설명 |
|---|---|---|---|
| S1 | SplashScreen | ✅ | 로딩 스플래시 |
| S0 | SavedListScreen | ✅ | 저장 리스트 · 출발일 뱃지 · 카드 탭→상세 |
| S6 | SavedDetailScreen | ✅ | 읽기전용 상세 · 지도/편집 진입 · **공유** · 삭제 |
| S2 | HomeScreen | ✅ | 직접/자동 설정 |
| S3a | TravelStyleScreen | ✅ | 테마(6종)·날씨·계절 |
| S3b | DetailConditionScreen | ✅ | 활동시간·인원·예산·짐·도착/출발 |
| S4 | SpotSelectScreen | ✅ | **테마 추천 정렬 + ★뱃지 + 전체 명소 선택** · 실사진(캐시) · AI 추천 · 기간 산출 · 숙소(키보드 회피) |
| S5 | TimelineScreen | ✅ | 타임라인 · **명소 편집(↑↓✕)** · 맛집 · 지도 진입 · 저장 |
| S5_2 | WeatherScreen | ✅ | 10일 예보 + 출발일 선택 → 저장 |
| S5_3 | RouteMapScreen | ✅ | 네이버 지도 · **실도로 경로(오렌지)** · 숙소🏠/번호핀 · **겹침 자동 해소** · 핀 말풍선 |
| S5_1 | BusinessHoursScreen | ✅ | 영업시간/정보 링크 |

### 화면 흐름
```
S0 ─카드탭→ S6 상세 ─편집→ S5    S0 ─새 여행→ S2 → S3a → S3b → S4 → S5
S5 ─저장→ S5_2 날씨(출발일) → S0    S5/S6 ─지도→ S5_3
```

---

## 4. 핵심 알고리즘 & 추천 로직

- **haversine / nearestNeighbor / calcTripDays(÷0 가드) / generateTimeline**
  - generateTimeline: 계절×날씨 보정, **누락 없이 전부 배치**, `manualSpotOrder` 시 사용자 순서 존중
- **테마 추천** (`constants/themeProfiles.ts`): 테마 = 차단 필터가 아니라 **추천 우선순위**
  - 카테고리(+2) + 성격 태그(+2) 점수 → 상단 정렬 + "★ 테마 맞춤" 뱃지, 나머지 전체 명소도 선택 가능
- **명소 병합** (`utils/mergeSpots.ts`): 시드 61곳(태그·체류시간) 기본 + TourAPI 보완, 이름 유사 중복 제거

---

## 5. 외부 API 연동 (`src/api/`)

| 파일 | 역할 |
|---|---|
| tourApi.ts | 명소 조회 · **이미지 보강**(⚠️ searchKeyword2+areaCode 조합은 일부 제주 명소 0건 — 필터 없이 검색 후 주소로 제주 판별) |
| weatherApi.ts | 오늘 날씨 + 10일 예보(단기+중기, allSettled 폴백) |
| naverMapApi.ts | 지오코딩 · 길찾기 요약 · **도로 경로 좌표**(AbortSignal) |
| mealRecommend.ts / aiApi.ts | 맛집 추천 · AI 코멘트/명소 추천 |

- API 키는 `.env`(EXPO_PUBLIC_*) 일원화, `.env.example` 제공

---

## 6. 테스트 현황 — `npm test` (60 tests / 11 suites)

- algorithms: haversine · nearestNeighbor · timeBudget · generateTimeline(누락방지·manualSpotOrder·overflow)
- constants: themeProfiles(점수·부분일치·복수테마)
- utils: date · shareSchedule · mergeSpots(중복판정·시드보존) · **seasonScore(계절별 우대·감점)**
- storage: scheduleStorage · imageCache
- 미커버: 컴포넌트 렌더, E2E

---

## 7. 작업 세션 이력 (2026-07)

**1차 (기능 구축)**
1. 날씨 기능 병합 (10일 예보 + WeatherScreen) 2. 리뷰 반영 3. 재저장 복제 정책 4. API 키 env 이전 5. RouteMap 동선 지도 6. SavedDetail(S6) 7. 출발일 표시 8. 알고리즘 테스트 + 누락 버그 수정 + 기간 정합 9. 시드 61곳 10. 명소 실사진 11. 심화 리뷰 반영(블로커 5) 12. StateView 13. 일정 텍스트 공유 14. storage/date 테스트

**2차 (QA 피드백 + 고도화)**
15. 숙소 입력 키보드 가림 수정 (KeyboardAvoidingView)
16. 지도 구분 개선 — 숙소 🏠 흰핀 / 동선 오렌지+흰 외곽선 / 범례 일치
17. 마커 겹침 자동 해소 — 픽셀 기준 fan-out, 줌 반응
18. Timeline 명소 편집 (↑↓✕, manualSpotOrder)
19. 앱 아이콘·스플래시 브랜딩 (물결 로고, app.json)
20. 테마 성격 강화 — themeProfiles 점수제 + ★뱃지
21. 테마 = 추천으로 전환 — 전체 명소 노출 + 상단 추천 정렬
22. 시드+TourAPI 병합 (대체→보완, 이름 중복 제거)
23. 이미지 미획득 원인 규명 — **TourAPI areaCode 필터 버그** 발견·우회
24. 명소 사진 AsyncStorage 캐시 (재방문 즉시 표시, API 절감)
25. SpotSelect 리팩토링 — SpotCard 컴포넌트(React.memo 성능 최적화)·seasonScore 유틸 분리, 960→864줄, 회귀 0
26. EAS 빌드 준비 — eas.json(3프로필: dev/preview/production), 번들ID `com.gyeoldaero.app`, `EAS빌드.md` 가이드

---

## 8. 실기기 QA 결과 (시뮬레이터 확인됨)

| 항목 | 결과 |
|---|---|
| 기본 구동 · S0 진입 | ✅ |
| 실시간 날씨 배지 (기상청) | ✅ |
| 숙소 지오코딩 (네이버) | ✅ |
| 명소 실사진 표시 | ✅ |
| 자동 담기 (테마 맞춤) | ✅ |
| **지도 렌더 + 실도로 경로** | ✅ |
| 잔여: 10일 예보→출발일 저장 · 공유 시트 · 핀 말풍선 · 편집 ↑↓✕ · 복제 저장 · 새 지도 스타일 | ⬜ |

---

## 9. 알려진 이슈 / 제약

- **TourAPI 데이터 불일치**: `searchKeyword2 + areaCode=39`에서 일부 제주 명소 0건 → 필터 없이 검색 후 주소로 판별 (우회 완료)
- 관광공사 DB에 사진 미등록 명소는 이모지 폴백 (정상)
- 이미지 캐시는 성공 URL만 저장 — 실패 명소는 화면 재진입 시 재시도
- Expo Go에선 커스텀 앱 아이콘·스플래시 미표시 (EAS 빌드에서 확인)
- `.env`의 `EXPO_PUBLIC_SERVER_URL`은 Mac LAN IP 변경 시 갱신 필요
- 시뮬레이터 화면 키보드: `⌘⇧K` (하드웨어 키보드 토글)

---

## 10. 남은 작업 (TODO)

### 🟡 기능/완성도
- [ ] QA 잔여 항목 확인 (8번 표의 ⬜)
- [ ] **EAS 빌드 실행** — 설정 완료, `EAS빌드.md` 따라 `npx eas-cli login → init → build` (Expo 계정 필요)
- [ ] 일정 이미지 공유 (view-shot)

### 🟢 품질
- [x] SpotSelect 리팩토링 (SpotCard·seasonScore 분리 — 2026-07-08 완료)
- [ ] 컴포넌트/통합 테스트

---

## 11. 폴더 구조 (요약)

```
gyeoldaero-main/
├── App.tsx / app.json(브랜딩·번들ID) / eas.json / .env(.example) / jest.config.js
├── assets/                 # 물결 로고 아이콘·스플래시
├── src/
│   ├── screens/            # 11개 화면
│   ├── components/         # RangeSlider, RouteMapWebView, SpotCard, StateView, WaveLogo
│   ├── algorithms/         # haversine, nearestNeighbor, timeBudget, generateTimeline (+tests)
│   ├── api/                # tour, weather, naverMap, mealRecommend, ai
│   ├── storage/            # scheduleStorage, imageCache (+tests)
│   ├── utils/              # date, shareSchedule, mergeSpots, seasonScore (+tests)
│   ├── constants/          # theme, themeProfiles(+tests), apiKeys, naverApiKeys
│   ├── types/  └── data/   # index.ts / jejuSpots(61곳)
├── 기록.md / 점검리스트.md / EAS빌드.md
```

---

## 12. 자주 쓰는 명령어

```bash
npx tsc --noEmit                      # 타입 체크
npm test                              # 유닛 테스트 (60개)
npx expo start                        # 개발 서버 (--ios: 시뮬레이터)
npx expo export --platform ios        # 번들 무결성 검증
npx eas-cli build -p ios --profile preview   # EAS 빌드 (EAS빌드.md 참고)
```
